<!DOCTYPE html>
<html>
<head>
  <title>UftResearchPortal</title>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class='navbar navbar-default navbar-static-top'>
  <nav class='container' role='navigation'>
    <div class='navbar-header'>
      <a href='/' class='navbar-brand'>University of Florida | Tallahassee</a>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='.navbar-collapse'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
    </div>
    <ul class='nav navbar-nav navbar-right collapse navbar-collapse'>
      <% if current_user %>
        <li><a href="/users/<%= current_user.id %>">Profile</a></li>
        <li><a href="/sessions/destroy">Log Out</a></li>
      <% else %>
        <li><a href="/sessions/new">Log In</a></li>
        <li><a href="/users/new">Register</a></li>
      <% end %>
    </ul>
        <div id="search_bar">
          <%= form_tag(proposals_path, :method => "get", class: "navbar-form", id: "search-form")do %>
          <span class="input-append">
            <button class="btn btn-default pull-right" type="submit">Search</button>
            <%= text_field_tag :search, params[:search], class: "span2 pull-right", placeholder: "Search By Department" %> 
          </span>
          <% end %>
        </div>
        
  </nav>
  </header>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>

