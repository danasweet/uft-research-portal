<%= form_for [@proposal, @experiment, @comment], url: "/proposals/:proposal_id/experiments/:experiment_id/comments", remote: true do |f| %>
  <%= f.text_field :content %>
  <%= f.text_field :stage %>
  <%= f.button :submit %>
<% end %>
