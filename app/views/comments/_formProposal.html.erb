<h4>Comment on this proposal</h4>

<%= form_for [@proposal, @comment], remote: true do |f| %>
  <%= f.text_area :content, size: "80x8" %>
  <p><%= f.button :submit, class: 'btn btn-default', class: 'btn btn-default' %></p>
<% end %>
